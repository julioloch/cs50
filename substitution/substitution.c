#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    if (argc = 0){
        printf("Usage: ./substitution key\n");
    } else if (argc > 1){
        printf("Key must contain 26 characters.\n");
    } else {
        if (strcom())
        string key = argv[1];
    }

    printf("%s\n", argv[1]);
    return 0;
}
