#include <cs50.h>
#include <stdio.h>

int main(void)
{
    string textInput = get_string("Text: ");
    int wordsTotal = 0;
    int sentencesTotal = 0;

    while (textInput[] != "\0"){
        
    }
    printf("%s\n", textInput);
}
